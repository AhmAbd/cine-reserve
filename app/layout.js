import '@styles/globals.css'

import { title } from 'process'

export const metadata = {
  title: "test",
  description: "Test"
}

const layout = () => {
  return (
    <div>layout</div>
  )
}

export default layout